 # AUDIT DREAMWEAVE — 29/04/2026

> Audit complet : Architecture · Base de données · Dead code · Sécurité · Performance · Évolutions en cours
> Produit par Claude Code (Sonnet 4.6) via agent général — 91 appels outils, lecture exhaustive du codebase.

---

## 1. ARCHITECTURE

### État général

L'architecture est solide et cohérente. La séparation en couches (`types` → `integrations` → `services` → `hooks` → `components` → `pages`) est respectée. Le lazy loading via `React.lazy` est appliqué sur toutes les pages. React Query gère le cache serveur avec des `staleTime` adaptés. Les Edge Functions Deno sont bien séparées de la logique client.

### Points notables

- La migration `20260424100000_rename_panels_to_chapter_canvases.sql` a renommé la table `panels` en `chapter_canvases`. Le code client utilise désormais `chapter_canvases` partout. Le type `Panel = Tables<"chapter_canvases">` dans `types/index.ts` est un alias sémantique correct.
- `generate-scenario-ai` a migré de Groq (Llama 3.3 70B) vers **Google Gemini Flash** (+ fallback Groq). Le `CLAUDE.md` indique encore "Groq — Llama 3.3 70B" → **à corriger**.
- `callNarraMindUpdate` dans `scenarioAI.ts` est marquée `@deprecated` mais pas supprimée.
- Deux fichiers `use-toast` : `src/hooks/use-toast.ts` (source) et `src/components/ui/use-toast.ts` (re-export). Redondant mais inoffensif.

---

## 2. BASE DE DONNÉES

### Tables & colonnes

| Table | Statut |
|-------|--------|
| `profiles` | ⚠️ `email` et `stripe_customer_id` absents des types générés |
| `assets` | ✅ Cohérent |
| `chapters` | ✅ Cohérent |
| `chapter_canvases` | ⚠️ `background_style` absent des types générés (statut incertain post-rename) |
| `projects` | ✅ Cohérent (incl. `universe_lore`) |
| `scenario_chapters` | ✅ Cohérent (incl. `ai_summary`, `locked_blocks`, `word_mappings`) |
| `usage` | ✅ Cohérent |
| `scenario_versions` | ❌ Pas de politique RLS visible dans les migrations |
| `memory_entities` / `memory_summaries` / `narramind_metrics` | ⚠️ Tables présentes en DB, absentes des types générés TS |

**Action requise** : Régénérer `src/integrations/supabase/types.ts` :
```bash
supabase gen types typescript --linked > src/integrations/supabase/types.ts
```

### RLS

| Table | Statut |
|-------|--------|
| `profiles` | ✅ Durcie 20260418 : `plan` non modifiable par client JWT |
| `projects`, `assets`, `chapters`, `chapter_canvases` | ✅ |
| `usage` | ✅ SELECT + INSERT seulement |
| `scenario_chapters`, `scenario_versions` | `scenario_versions` : ❌ aucune `CREATE POLICY` visible |
| `memory_entities`, `memory_summaries`, `narramind_metrics` | ✅ Basées sur `auth.uid()` |

### Migrations — Ordre et risques

- Ordre cohérent, pas de dépendance circulaire.
- `reorderChapters` / `reorderScenarioChapters` : N UPDATE séquentiels — condition de concurrence possible avec 2 utilisateurs simultanés.
- Pattern FK NarraMind corrigé par `20260423200000_fix_narramind_fk.sql` (référençait `profiles(id)` au lieu de `auth.users(id)` initialement).

---

## 3. ROUTES & PAGES

### Pages actives

| Route | Composant | Lazy | Protégée |
|-------|-----------|------|----------|
| `/` | `Landing` | ✅ | ❌ |
| `/auth` | `Auth` | ✅ | ❌ |
| `/auth/verify-email` | `EmailVerification` | ✅ | ❌ |
| `/auth/reset-password` | `ResetPassword` | ✅ | ❌ |
| `/dashboard` | `Dashboard` | ✅ | ✅ |
| `/dashboard/projects` | `Projects` | ✅ | ✅ |
| `/dashboard/projects/:id` | `ProjectDetail` | ✅ | ✅ |
| `/dashboard/projects/:id/scenario/:chapterId` | `ScenarioChapterEditor` | ✅ | ✅ |
| `/dashboard/projects/:id/chapter/:chapterId` | `ChapterDetail` | ✅ | ✅ |
| `/dashboard/profile` | `Profile` | ✅ | ✅ |
| `/dashboard/plans` | `Plans` | ✅ | ✅ |
| `*` | `NotFound` | ✅ | ❌ |

**Aucune dead page** : toutes les pages dans `src/pages/` correspondent à une route déclarée.

---

## 4. BUGS & INCOHÉRENCES

### P0 — Bloquant (sécurité / données)

**[B0-1]** `src/integrations/supabase/types.ts` désynchronisé
- Colonnes `email`, `stripe_customer_id` (profiles) et `background_style` (chapter_canvases) absentes des types TS.
- Impact utilisateur : accès TypeScript impossible sans cast, Stripe silencieusement cassé côté types.
- Fichier : `src/integrations/supabase/types.ts` → régénérer.

**[B0-2]** ✅ FAUX POSITIF (Phase 8 — 30/04/2026) `scenario_versions` sans RLS explicite
- Vérification : la migration `20260214200000_add_scenario_chapters.sql` définit bien `ENABLE ROW LEVEL SECURITY` + 4 politiques (SELECT/INSERT/UPDATE/DELETE) filtrées par `auth.uid() = user_id`. Table correctement sécurisée depuis sa création. Aucune action requise.

### P1 — Important (fonctionnalité cassée)

**[B1-1]** ✅ FAUX POSITIF (vérifié 30/04/2026) `generate-panel-image` ne loggue pas l'usage
- Vérification : l'INSERT `usage` est présent aux lignes 825-834 de `generate-panel-image/index.ts` (fire-and-forget). Table correctement alimentée depuis au moins la Phase 11. Aucune action requise.

**[B1-2]** ✅ FAUX POSITIF (vérifié 30/04/2026) Pas de vérification quota côté client pour les blocs panels
- Vérification : `ChapterDetail.tsx` ligne 971 contient bien `if (usageInfo.count >= usageInfo.limit) { setShowQuotaModal(true); return; }` avant tout appel à `generatePanelImage.mutate`. Modal quota dédiée implémentée. Aucune action requise.

**[B1-3]** `callNarraMindUpdate` marquée deprecated mais exportée
- Risque de confusion future si un développeur l'utilise par erreur.
- Fix : supprimer la fonction et le type `NarraMindAIRequest` de `scenarioAI.ts`.

### P2 — Moyen (UX dégradée)

**[B2-1]** ✅ CORRIGÉ (Phase 9 — 30/04/2026) Email admin hardcodé dans le bundle JS public
- Fix : `user?.email === import.meta.env.VITE_ADMIN_EMAIL` — l'email est maintenant dans `.env.local` (non commité). En production si `VITE_ADMIN_EMAIL` n'est pas défini dans les secrets Vercel, le bandeau admin est invisible. Aucun email en dur dans le bundle.

**[B2-2]** ✅ COMPORTEMENT INTENTIONNEL (vérifié 30/04/2026) `CANVAS_HEIGHT = 50_000` dans `ChapterDetail.tsx`
- Vérification : `CANVAS_HEIGHT = 50_000` est utilisé uniquement pour le layout initial du premier canvas auto-créé lors de l'ouverture d'un chapitre vide. `PANEL_HEIGHT_DEFAULT = 5000` est un fallback de lecture pour les panels sans `panelHeight`. Ce sont deux concepts distincts (surface de création vs valeur par défaut de lecture). `PANEL_HEIGHT_MAX = 100_000` confirme que 50 000 est dans les bornes. Aucune action requise.

**[B2-3]** ✅ FAUX POSITIF (vérifié 30/04/2026) Page Plans incorrecte
- Vérification `Plans.tsx` : résolution affichée `"1280×1024"` (ligne 67), feature "Sheet System 4 angles" correctement listée (ligne 58-63), "Vues multiples" supprimée. Page cohérente avec la réalité. Aucune action requise.

**[B2-4]** ✅ CORRIGÉ (Phase 11 — 30/04/2026) `duration_ms` mesuré via `Date.now()` avant/après l'appel Gemini dans `narramind-update/index.ts`. ⚠️ Deploy EF requis.

### P3 — Faible (polish)

**[B3-1]** `_suggestingBlockKeys` state (`ChapterDetail.tsx:180`) — setter utilisé, valeur jamais lue.

**[B3-2]** ✅ CORRIGÉ (Phase 10 — 30/04/2026) `reorderChapters` / `reorderScenarioChapters` — upsert batch, 1 requête au lieu de N.

**[B3-3]** ✅ CORRIGÉ (Phase 12 — 30/04/2026) `html2canvas` + `JSZip` lazy-loadés via `import()` dynamique au clic Export. ~250 KB retirés du bundle initial.

---

## 5. DEAD CODE

| Fichier | Symbole | Raison |
|---------|---------|--------|
| `src/components/project/PanelEffectsMenu.tsx` | Composant entier | Non importé nulle part dans `src/` |
| `src/components/project/PanelCountBadge.tsx` | Composant entier | Non importé nulle part dans `src/` |
| `src/services/scenarioAI.ts` | `callNarraMindUpdate()` | `@deprecated`, plus appelée nulle part |
| `src/services/scenarioAI.ts` | `NarraMindAIRequest` dans union `AIRequest` | Mode `narramind` déplacé vers Edge Function `narramind-update` |
| `src/pages/ChapterDetail.tsx:180` | `_suggestingBlockKeys` state | Setter utilisé, valeur jamais lue |
| `src/types/index.ts` | `allowMultipleViews: boolean` dans `TierLimits` | Feature supprimée (Sheet System), champ jamais consulté |

---

## 6. ÉVOLUTIONS EN COURS (non finalisées)

### NarraMind
- **État** : tables DB + Edge Function `narramind-update` fonctionnelles. Déclenché auto depuis `ScenarioChapterEditor` (5 min + ≥50 mots) et `UniverseSection` (save lore).
- **Manque** : aucun composant frontend pour visualiser les entités mémorisées. Types TS absents.
- **Fichiers** : `supabase/functions/narramind-update/`, `src/services/scenarioAI.ts`, `src/pages/ScenarioChapterEditor.tsx`, `src/components/project/UniverseSection.tsx`

### Stripe / Plans
- **État** : webhook déployé, `create-checkout-session` et `create-portal-session` fonctionnels, `stripe.webhooks.constructEventAsync` vérifié.
- **Manque** : vérification que `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `price_id` Stripe et URLs success/cancel sont configurés dans les secrets Supabase. Aucun test end-to-end visible.
- **Fichiers** : `supabase/functions/stripe-webhook/`, `supabase/functions/create-checkout-session/`, `src/hooks/useUserPlan.ts`, `src/pages/Plans.tsx`

### admin-set-plan
- **État** : fonctionnel pour `kiritogeek@gmail.com`.
- **Manque** : à supprimer/sécuriser avant release prod.

### background_style sur chapter_canvases
- **État** : colonne ajoutée par `20260218`, non mentionnée dans la migration rename `20260424`. Statut en production incertain.
- `PanelEffectsMenu.tsx` l'utilise via cast — mais ce composant est dead code.

### Bulles validation progressive
- **État** : 2/13 validées (speech, shout). Masquées temporairement dans l'éditeur.

---

## 7. SÉCURITÉ

### Confirmé correct ✅

- JWT vérifié dans toutes les Edge Functions productrices (`generate-asset-image`, `generate-panel-image`, `generate-scenario-ai`, `narramind-update`, `admin-set-plan`).
- Aucune clé API (FAL, Groq, Gemini, Stripe) exposée côté client.
- `VITE_SUPABASE_PUBLISHABLE_KEY` est une anon key publique — exposition normale.
- RLS activée sur toutes les tables principales.
- `profiles.plan` protégé par migration 20260418 (non modifiable par client JWT).
- `refreshSession()` appelé avant chaque Edge Function depuis le client.
- Signature Stripe vérifiée via `stripe.webhooks.constructEventAsync`.
- Ownership double-vérifié dans les Edge Functions.

### Vulnérabilités identifiées ❌

| Sévérité | Bug | Description |
|----------|-----|-------------|
| P0 | B0-2 | `scenario_versions` sans RLS explicite |
| P1 | B1-1 | Quota panel image non compté → abus possible |
| P2 | B2-1 | Email admin exposé dans bundle JS |

---

## 8. PERFORMANCE

### Re-renders
- `ChapterDetail.tsx` (~1700 lignes) centralise un state lourd. Chaque frappe dans un prompt déclenche un re-render du composant parent. Les sous-composants `ColorBlockLayer`, `ImageBlockLayer`, `BubbleLayer` étant extraits, l'impact est atténué, mais les callbacks en props créent encore des re-renders en cascade.

### Requêtes N+1
- `reorderChapters` / `reorderScenarioChapters` : N UPDATE séquentiels → À remplacer par `upsert([...], { onConflict: "id" })`.
- `generate-panel-image` : 3 requêtes DB en cascade (canvas → chapter → project) → À remplacer par une jointure SQL unique.

### Bundle
- `html2canvas` + `JSZip` chargés statiquement dans `ChapterDetail` → ~250 KB inutiles au chargement. À lazy-loader au clic Export.
- `framer-motion` importé dans `Landing.tsx` et `Plans.tsx` (lazy-loadés) → impact limité.

### Images
- PNG générés par FAL.ai servis directement depuis Supabase Storage. Pas de WebP, pas de redimensionnement adaptatif, pas de CDN d'images.
- Sheet images (2560×768 PNG) servies sans lazy loading dans `CharacterViewDialog.tsx`.

### Cache React Query
- `usePanels` : `staleTime: 5 min`. Deux sessions simultanées sur le même chapitre ne voient pas les modifications avant 5 min.

---

## 9. ARCHITECTURE OPTIMALE À ADOPTER

### Immédiat (P0/P1)

1. **Régénérer `src/integrations/supabase/types.ts`** → règle B0-1
2. **Ajouter RLS sur `scenario_versions`** (migration SQL) → règle B0-2
3. **Logger l'usage dans `generate-panel-image`** → règle B1-1 (impact économique direct)
4. **Vérifier quota côté client dans `ChapterDetail`** avant génération bloc → règle B1-2

### Court terme (P2/dead code)

5. Supprimer `PanelEffectsMenu.tsx`, `PanelCountBadge.tsx`, `callNarraMindUpdate`, `NarraMindAIRequest`, `_suggestingBlockKeys`
6. Corriger page Plans (features + résolution)
7. Lazy-loader `exportPanel.ts` (html2canvas + JSZip)
8. Clarifier `CANVAS_HEIGHT = 50_000` vs `PANEL_HEIGHT_DEFAULT = 5000`
9. Mettre à jour `CLAUDE.md` : modèle IA Scénario = Google Gemini Flash

### Moyen terme

10. Batch les reorders via `upsert`
11. Mesurer `duration_ms` dans NarraMind metrics
12. Frontend pour visualiser les entités NarraMind (onglet Univers)
13. CDN images ou transformation WebP pour les assets FAL.ai

---

---

## 10. PHASES 13–14 (30/04/2026)

### Phase 13 — Visualisation scénario formatée

**[B-UX-1]** ✅ CORRIGÉ (Phase 13 — 30/04/2026) Texte scénario affiché avec marqueurs Markdown bruts dans la sidebar de `ChapterDetail`
- Symptôme : le panneau Scénario affichait `###`, `>` et autres marqueurs visibles au lieu d'un texte formaté.
- Fix : création de `src/components/project/ScenarioFormattedPreview.tsx` — composant de lecture formatée qui :
  - Supprime les préfixes `###` et `>` et applique les styles appropriés (heading violet, métadonnées cyan/violet, dialogues italiques)
  - Conserve le surlignage des assets par type dans chaque ligne
- `ChapterDetail.tsx` remplace `<ScenarioTextHighlighter>` par `<ScenarioFormattedPreview>` dans le panneau lecture.

### Phase 14 — Documentation NarraMind

**[DOC-1]** ✅ CRÉÉ (Phase 14 — 30/04/2026) `Produit/NarraMind.md`
- Document complet sur le système NarraMind : concept, architecture, flux de traitement, modèle de données, sécurité, budget contexte, état actuel, vision produit, fichiers clés.
- `Produit/INDEX.md` mis à jour pour référencer NarraMind.

---

### Phase 15 — Amélioration contenu des cases + bouton "Tout appliquer"

**[UX-2]** ✅ CORRIGÉ (Phase 15 — 30/04/2026) Cases générées sans contexte assets

- Symptôme : `handleDetectBlocks` ne passait pas les assets ni le lore du projet à l'IA → descriptions génériques sans noms d'assets exacts.
- Fix multi-couches :
  - `DetectBlocksRequest` enrichi avec `assets_context` et `universe_lore` (facultatifs)
  - `buildDetectBlocksPrompt` inclut désormais les assets (nom + type + description courte, max 20) et le lore
  - Edge Function `generate-scenario-ai` passe les nouveaux champs à `buildDetectBlocksPrompt`
  - `ScenarioChapterEditor.handleDetectBlocks` construit et envoie le contexte assets + lore
  - `target_panel_count` (déjà dans les types) désormais aussi passé à l'IA
- `EditorRightPanel` : bouton "Tout appliquer (N cases)" ajouté — crée d'un clic tous les blocs non encore ajoutés
- `ChapterDetail` : handler `handleAddAllCases` créé (batch de tous les blocs unadded)

**[UX-3]** ✅ CORRIGÉ (Phase 15 — 30/04/2026) Texte scénario brut dans `EditorRightPanel`
- Symptôme : onglet "Scénario" du panneau droit de `ChapterDetail` utilisait `ScenarioTextHighlighter` → `###` et `>` visibles.
- Fix : remplacé par `ScenarioFormattedPreview` dans `EditorRightPanel`.

---

*Audit réalisé le 29/04/2026 — lecture de 91 fichiers sources, migrations, Edge Functions*
*Mis à jour le 30/04/2026 — Phases 13–15*

project_id = "lbiqsodnvrnxdprcucmb"

# Permet d'appeler la fonction sans que la passerelle rejette le JWT (connexion Google / clés asymétriques).
# La fonction vérifie elle-même que l'utilisateur possède l'asset via le client authentifié.
[functions.generate-asset-image]
verify_jwt = false

# IA Scénario / Chapitre / Découpage panels : la fonction vérifie le JWT via /auth/v1/user (SUPABASE_ANON_KEY).
# Désactiver verify_jwt ici évite un 401 côté passerelle (token expiré / format) et laisse la fonction gérer l'auth.
[functions.generate-scenario-ai]
verify_jwt = false

[functions.generate-panel-image]
verify_jwt = false
# Audit Session — 2026-07-04
**Session :** Fiabilisation Univers/Ariane + UX scénario + refresh docs
**Statut final :** ✅ PASS

## Vérifications
- `npx tsc --noEmit` → **0 erreur**
- `npm run lint` (ESLint) → **0 warning**
- `npx vitest run` → **545 tests / 32 fichiers, 0 échec**

## ✅ Accompli

**Univers / Ariane**
- Bug apparition graph corrigé (queryKey `lore-nodes`→`lore_nodes`) — `useArianeLoreProposals.ts`
- Connexions Ariane : dédup vs `lore_edges` existants + filtrage par importance (co-mention même phrase, accent-aware) — `useArianeLoreProposals.ts`
- Auto-lien chapitre : accents (`contentContainsAssetName`) + skip si déjà lié — `useArianeLoreProposals.ts`
- Tracé des liens : accroche axe dominant + couloirs par paire — `LoreGraphView.tsx`
- Overlay onboarding « Construis ton Univers » retiré — `LoreGraphView.tsx`
- Faux « assets manquants » masqués (filtre client) — `ArianeContinuityPanel.tsx`

**Scénario / éditeur**
- Chapitre 1 pré-amorcé par le synopsis — `ScenarioSection.tsx` ; Ariane ch.1 fondée sur synopsis — `generate-scenario-ai` (EF, **à déployer**)
- Création d'asset depuis le scénario via pop-up partagée — `CreateAssetDialog.tsx`, `AssetLibrary.tsx`, `ScenarioChapterEditor.tsx`
- Réglages découpage en popover au clic « Découper » — `ScenarioChapterEditor.tsx`

**Paramètres / offre**
- Suppression de projet dans Paramètres + responsive — `ProjectSettingsSection.tsx`
- Badge « Couverture à générer » recentré — `DashboardHome.tsx`
- Card Studio : retrait des features non livrées (mémoire longue, priorité FAL) — `Plans.tsx`
- Commentaire `PANEL_HEIGHT` corrigé (max 100 000) — `types/index.ts`

**Docs** : refresh complet Wiki-Brain (roadmap + 8 pages auditées vs code) + 2 fiches mémoire.

## 🚨 Bloquants détectés
Aucun. Code vérifié (tsc + eslint + 545 tests verts). Zone Canvas protégée (blocs image/couleur/bulles) non touchée.

## 🐛 Bugs identifiés (non résolus)
- Faux « assets manquants » : corrigé côté **affichage** (filtre client) ; la SOURCE (`narramind-update` EF) persiste des faux positifs — fix serveur optionnel non déployé. Sévérité : mineur.
- Fixes graph B/D non vérifiés visuellement (pages derrière l'auth). Sévérité : mineur.

## ⚠️ Corrections appliquées
Toutes hors zone Canvas protégée. Risque de régression faible (client, tsc + eslint + 545 tests verts). ~15 commits poussés sur `pre-production` (`76caa1e`..`a7abb83`).

## 📌 Sujets à traiter (prochaine session)
- **P1 🔴** Câbler `world_rules` → IA (NarraMind + découpage) — `universe_lore` n'a plus de setter, `world_rules` non lu par l'IA (Edge Functions, feu vert requis)
- **P2 🟠** « Ajouter à l'univers » depuis le graph (boutons Accepter/Ignorer)
- **P2 🟠** Option A Phase 5 : refacto `ChapterDetail.tsx` (3066 → < 400) + `useCanvasSelection` + multi-sélection
- **P3 🟡** Fix serveur assets manquants (réconciliation `narramind-update`) + vérif visuelle graph

## 🔍 État du code
- TypeScript errors : 0
- Tests en échec : 0 (545 passés)
- Migrations Supabase en attente : non
- Edge Functions à déployer : `generate-scenario-ai` (Ariane ch.1 synopsis) ; optionnel `narramind-update`

## 📎 Contexte à retenir
- **queryKey lore = underscore** (`lore_nodes`), jamais tiret.
- **Matching de noms = `contentContainsAssetName`** (accent-aware), jamais `\b` (ASCII).
- **Écart archi** : `universe_lore` (consommé par IA, setter retiré) vs `world_rules` (bouton Règles du Monde, non lu par l'IA) — décision de câblage en attente.
- **CORS** : dev local tape les EF de prod ; `ALLOWED_ORIGIN` doit inclure `localhost:8080` + `127.0.0.1:8080`.
- Diagnostics produits par 4 workflows multi-agents (contre-vérification adversariale).
- Tout poussé sur `pre-production`. **`main` NON poussé** (Louis a dit « sur la preprod », pas « mets en prod »).

# Audit — DreamWeave
**Date :** 22/05/2026
**Session :** NarraMind Compass Phase 1 (indexation vectorielle pgvector) + Phase 2 (UI Univers restructuré + suggestions Ariane)
**Statut final :** ✅ PASS

## TypeScript
- [x] `npx tsc --noEmit` → 0 erreur

## Tests
- [x] `npm test` → 50/50 PASS (4 fichiers)

## Ce qui a été livré

### NarraMind Compass Phase 1 — Indexation vectorielle
- Extension pgvector activée en prod
- Table `project_embeddings` (RLS, index unique expression COALESCE, match_embeddings())
- Table `compass_proposals` (RLS, dedupe_key)
- Edge Function `narramind-compass` déployée (modes `index` + `propose`)
- Service `compassIndex.ts` + hook `useCompassIndex` (fire-and-forget)
- Branchements silencieux : chapitre auto-save, lore monde, LORE asset
- Types `CompassProposal`, `ProjectEmbedding`, `CompassSourceType`, `CompassProposalType`

### NarraMind Compass Phase 2 — UI Univers
- 5 colonnes `lore_*` ajoutées dans `projects` (migration appliquée)
- `UniverseSection.tsx` restructuré : onglet Monde → 5 sections indépendantes
- `CompassSuggestionsPanel.tsx` : panneau suggestions 🔍 extraites / ✨ inventées
- `useCompassProposals.ts` : hook fetch + dismiss/accept
- Tier gate : plan libre → message non bloquant, créateur/studio → accès illimité
- Bandeau migration `universe_lore` → localStorage par projet
- `universe_lore` conservé et mis à jour (buildUniverseLore) pour compatibilité narramind-update

### Documentation
- `ProjetInnovant.md` WSF5 mis à jour : Itération 4 Compass, benchmarks vectoriels (§3.4), seuil minimal de données (§3.5)

## Bugfix en cours de session
- Migration `project_embeddings` : contrainte UNIQUE avec COALESCE() non supportée inline → remplacée par `CREATE UNIQUE INDEX` avec expression
- Variable `now` non utilisée dans l'EF narramind-compass → supprimée (warning ESLint bloquait le commit)

## Sujets à aborder en prochaine session
1. **Retravailler les sections lore monde** : les labels actuels (Magie, Géographie, Factions, Culture, Chronologie) sont trop spécifiques — tous les projets n'ont pas de magie. Proposer des sections plus génériques ou un système à la carte que l'utilisateur active.
2. **Phase 3 Compass** : bandeau "Ariane a analysé N chapitres" sur fiches assets (AssetLibrary)
3. **Phase 4 Compass** : "Proposition Ariane" → 3 directions narratives dans ScenarioChapterEditor
4. **Phase 5 Compass** : pré-remplissage formulaire création asset (états A/B)
5. **Merger main dans pre-production** : main a récupéré des commits (Dashboard, Landing, Projects refonte) que pre-production n'avait pas — fusionner proprement en début de prochaine session

## Décisions prises
- Vectorisation silencieuse (pas de feedback visuel), même pattern que narramind-update
- `universe_lore` conservé pour compatibilité EF narramind-update — non modifiable sans déploiement EF
- Schéma BDD : 5 colonnes dans `projects` (vs table dédiée) — simplicité > flexibilité en MVP
- EF `narramind-compass` créée avec modes `index` + `propose` d'emblée (pas en deux fois)
- Tier gate Phase 2 : plan libre → message non bloquant (pas de blocage dur)
